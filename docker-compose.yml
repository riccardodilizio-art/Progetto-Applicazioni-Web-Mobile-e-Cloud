services:
  frontend:
    build:
      context: ./Frontend-hotel-site
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped


  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   restart: unless-stopped
