services:
  api:
    build:
      context: ./Backend-hotel-site
      dockerfile: Hotel.Site.Api/Dockerfile
    ports:
      - "8081:8080"
    environment:
      - ConnectionStrings__DefaultConnection=${DB_CONNECTION}
      - Smtp__Host=${Smtp__Host}
      - Smtp__Port=${Smtp__Port}
      - Smtp__Username=${Smtp__Username}
      - Smtp__Password=${Smtp__Password}
      - Smtp__FromEmail=${Smtp__FromEmail}
      - Smtp__FromName=${Smtp__FromName}
      - Smtp__AdminEmail=${Smtp__AdminEmail}
      - App__FrontendUrl=${App__FrontendUrl}
      - Jwt__Key=${Jwt__Key}
    volumes:
      - ./uploads:/app/wwwroot/uploads
      - ./dp-keys:/app/dp-keys
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:8080/api/rooms || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  frontend:
    build:
      context: ./Frontend-hotel-site
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy


